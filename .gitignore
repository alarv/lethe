node_modules/
dist/
*.tsbuildinfo
.env
.env.*

# Derived index. Rebuilt from .lethe/memory/*.md via `lethe index`.
# The markdown is the source of truth; this is a build artifact.
.lethe/index.db
.lethe/index.db-journal
.lethe/index.db-wal
.lethe/index.db-shm
.lethe/models/

# Local research notes, not part of the published project.
docs/competition.md

.DS_Store
coverage/
evals/results/

# Chrome renders at 2x; keep the committed PNG but not scratch renders
assets/*.tmp.png

# lethe memories stay on this machine; config.json is shared
.lethe/memory/

# Session handoff note, not part of the project.
docs/lethe.md

# Design specs. Internal working documents -- they argue with themselves,
# name unbuilt work, and record rejected options, none of which reads well
# to someone finding the repository. The decisions that matter get written
# up in docs/ and in commit messages instead.
docs/superpowers/
